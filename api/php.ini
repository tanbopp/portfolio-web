; vercel-php runtime overrides (applied during build)
memory_limit = 256M
max_execution_time = 60
upload_max_filesize = 20M
post_max_size = 20M
; write temp/session data to the ephemeral /tmp (serverless)
upload_tmp_dir = /tmp